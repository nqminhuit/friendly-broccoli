name: Ubuntu base image

run-name: Build and push Ubuntu base image version ${{inputs.version || '24.04'}}

on:
  workflow_dispatch:
    inputs:
      version:
        description: Ubuntu version
        required: true
        default: '26.04'
        type: choice
        options:
            - '24.04'
            - '25.10'
            - '26.04'
  schedule:
    - cron: '0 0 * * 0'

env:
  REGISTRY: docker.io
  VERSION: ${{inputs.version || '26.04'}}
  IMAGE_NAME: ubuntu-base

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Build Ubuntu ${{env.VERSION}} image base
        run: podman build --no-cache --build-arg VERSION=${{env.VERSION}} -f containers/Containerfile-${{env.IMAGE_NAME}} -t ${{env.IMAGE_NAME}}:${{env.VERSION}}
        shell: bash

      - name: Push ${{env.IMAGE_NAME}} image to ${{env.REGISTRY}}
        run: podman push --quiet --creds ${{secrets.DOCKER_IO_USERNAME}}:${{secrets.DOCKER_IO_TOKEN}} ${{env.IMAGE_NAME}}:${{env.VERSION}} docker://docker.io/nqminhuit/${{env.IMAGE_NAME}}:${{env.VERSION}}
        shell: bash
