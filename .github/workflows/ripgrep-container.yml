name: ripgrep-container-build

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'ripgrep version'
        required: true
        default: 15.2.0
      debian_version:
        description: 'Debian version'
        required: true
        default: 13

jobs:
  build:
    uses: ./.github/workflows/build-and-push-container.yml
    with:
      name: ripgrep
      version: ${{ inputs.version }}
      debian_version: ${{ inputs.debian_version }}
    secrets:
      DOCKER_IO_USERNAME: ${{ secrets.DOCKER_IO_USERNAME }}
      DOCKER_IO_TOKEN: ${{ secrets.DOCKER_IO_TOKEN }}
